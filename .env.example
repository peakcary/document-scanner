# 文档扫描器部署配置模板
# 复制此文件为 .env.local 并填入真实服务器信息

# 开发环境配置
DEV_HOST=your-dev-server-ip
DEV_USER=root
DEV_PASSWORD=your-password
# DEV_KEY_FILE=/path/to/your/ssh/key  # 推荐使用SSH密钥而不是密码
DEV_PATH=/var/www/document-scanner
DEV_PORT=22
DEV_URL=http://your-dev-server-ip:8080
# DEV_RESTART_CMD=systemctl restart nginx  # 可选的重启命令

# 生产环境配置
PROD_HOST=your-prod-server-ip
PROD_USER=deploy
PROD_PASSWORD=your-prod-password
# PROD_KEY_FILE=/path/to/your/ssh/key  # 推荐使用SSH密钥
PROD_PATH=/var/www/document-scanner
PROD_PORT=22
PROD_URL=https://your-domain.com
# PROD_RESTART_CMD=systemctl restart nginx

# 测试环境配置
STAGING_HOST=your-staging-server-ip
STAGING_USER=staging
STAGING_PASSWORD=your-staging-password
STAGING_PATH=/var/www/staging/document-scanner
STAGING_PORT=22
STAGING_URL=http://staging.your-domain.com:8080